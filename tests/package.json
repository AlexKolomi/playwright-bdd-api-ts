{
  "name": "playwright-bdd-api-test",
  "version": "1.0.0",
  "description": "BDD API testing using Playwright and Cucumber in TypeScript",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require 'features/**/*.ts' --publish"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.1.0",
    "@playwright/test": "^1.43.1",
    "@types/node": "^16.0.0",
    "dotenv": "^16.0.0",
    "playwright": "^1.20.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  }
}
